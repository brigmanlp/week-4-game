
$(document).ready(function() {

var crystalCollector = { //create an object for the Crystal Collector game and give each element a property.

    rnumber: 0, //random number generated by the computer
    wins:0, //wins counter tracker
    losses:0, //losses counter tracker
    crystal1: 0, //crystal 1 with random number 1-12
    crystal2: 0, //crystal 2 with random number 1-12
    crystal3: 0, //crystal 3 with random number 1-12
    crystal4: 0, //crystal 4 with random number 1-12
    totalScore: 0, //total score counter 
    // yourPick:0,

    startGame: function() {
        //set rnumber equal to value between 19 - 120; this is value that the player must total in the game
        crystalCollector.rnumber = Math.floor(Math.random() * 120) + 19;
        $("#rnumber").html(crystalCollector.rnumber);
        console.log("the computer random number is: " + crystalCollector.rnumber);

        //set crystal images to random numbers between 1-12:
        crystalCollector.crystal1 = Math.floor(Math.random() * 12) + 1;
        console.log("crystal-1: " + crystalCollector.crystal1);

        crystalCollector.crystal2 = Math.floor(Math.random() * 12) + 1;
        console.log("crystal-2: " + crystalCollector.crystal2);

        crystalCollector.crystal3 = Math.floor(Math.random() * 12) + 1;
        console.log("crystal-3: " + crystalCollector.crystal3);

        crystalCollector.crystal4 = Math.floor(Math.random() * 12) + 1;
        console.log("crystal-4: " + crystalCollector.crystal4);

        //reset the total score counter to zero
        crystalCollector.totalScore=0;

        //display current value of total score counter (zero) and wins/losses
        $("#wins").html(crystalCollector.wins);
        $("#losses").html(crystalCollector.losses);
        $("#totalScore").html(crystalCollector.totalScore);
    },
    //Next step is to check which crystal was clicked and assign the number value to each one.
    // and update total score and wins/losses
    crystalClick: function(event){

        var crystalClick = event.target.id; //In jQuery event.target always refers to the element 
        //that triggered the event, where 'event' is the parameter passed to the function
        crystalCollector.totalScore += crystalCollector[crystalClick];
        $("#totalScore").html(crystalCollector.totalScore);
        

        if(crystalCollector.totalScore === crystalCollector.rnumber){
            
            crystalCollector.wins++;
            $("#youLW").html("You Win!");
            crystalCollector.startGame();
            
        }
        else if(crystalCollector.totalScore > crystalCollector.rnumber){
            
            crystalCollector.losses++;
            $("#youLW").html("You Loose!");
            crystalCollector.startGame();
            
        }

         
    }, //this closing tag ties to crystalClick function


}; //this closing tag ties to variable crystalCollector

// document.addEventListener("DOMContentLoaded", function(){

crystalCollector.startGame();

// crystalCollector.startGame();

$('.crystalImg').click(crystalCollector.crystalClick);

});


